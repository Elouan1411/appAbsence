import React from "react";

function Separator({ children }) {
    return (
        <div className="add-separator">
            <span>{children}</span>
        </div>
    );
}

export default Separator;
