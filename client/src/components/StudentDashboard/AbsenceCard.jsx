import React, { useState } from "react";
import "../../style/Student.css";
import toast from "react-hot-toast";

const AbsenceCard = ({ subject, date, startTime, endTime, status, onAction }) => {
    return <div></div>;
};

export default AbsenceCard;
